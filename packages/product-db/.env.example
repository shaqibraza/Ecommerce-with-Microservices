# Used by Prisma CLI (db:generate, db:migrate, db:deploy)
# Copy to .env in this directory, or set the same value in apps/product-service/.env
DATABASE_URL=postgresql://user:password@localhost:5432/ecommerce
